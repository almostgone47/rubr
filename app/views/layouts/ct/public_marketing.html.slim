doctype html
html.no-js
  head
    title Crop Tech Solutions
    meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no"
    = render "layouts/favicons"
    = stylesheet_link_tag "application", media: "all"
    = stylesheet_link_tag "//fonts.googleapis.com/css?family=Raleway:400,300"
    /[if IE]
      = stylesheet_link_tag "no_ie", media: "all"
    = yield :head
    = csrf_meta_tags
  body
    div.app-wrapper
      = render partial: 'layouts/public_navigation'
      = render partial: 'layouts/header_bar_public'
      .header-print-logo
        = image_tag "CropTech-logo-color.png"
      .main-content
        .css-arrow#notification_arrow
        .container
          #content
            = render partial: 'layouts/messages'
            = yield
        /[if IE]
          #ie
            = render partial: 'layouts/no_ie'
    = javascript_include_tag "application"
    - if Rails.env.production?
      = javascript_include_tag "analytics"
    = yield :footer
